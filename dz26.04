#include <iostream>

using namespace std;

int main() {
    int* a, size;
    cout << "Massive size: ";
    cin >> size;
    a = new int[size];
    for (int i = 0; i < size; i++) {
        cin >> a[i];
    }
    for (int i = 0; i < size; i++) {
        cout << a[i] << " ";
    }
}
